#include "Polygon1.h"

void Polygon1::SetValues(int width, int height) {
    mWidth = width;
    mHeight = height;
}

// No implementation for Area() in Polygon.cpp since it's a pure virtual function
